# clippy, rls, and rustfmt is linter
# miri is the new rust ir interpreter for constexpr op
# nightly is used to enable some new features of rust/cargo
# system-llvm is there so that installed llvm is used, instead of compiling their own version
# rust-src is required by rls
dev-lang/rust clippy rls rustfmt miri nightly system-llvm wasm llvm_targets_WebAssembly rust-src
